<h2><%= t(:newissuealerts_new, :project => @project.name).html_safe %></h2>
<%= form_for @newissuealert, :url => newissuealerts_path(:project_id => @project) do |f| %>
  <%= error_messages_for 'newissuealert' %>
  <%= render :partial => 'newissuealerts/form', :locals => { :f => f} %>
<% end %>
